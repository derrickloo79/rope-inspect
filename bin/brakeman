#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Do not pass --ensure-latest: Brakeman 8+ requires Ruby >= 3.2, while this
# app targets Ruby 3.1.4. Forcing "latest" makes CI fail even when the scan
# itself is clean.
load Gem.bin_path("brakeman", "brakeman")
