<% content_for :title, "Edit #{@fsp.fsp_number} — Hey Tec Inspect" %>

<div class="space-y-6 max-w-xl">
  <div>
    <%= link_to "← #{@fsp.fsp_number}", dashboard_fsp_path(@fsp), class: "text-sm no-underline text-ink-muted hover:text-ink" %>
    <h1 class="page-title mt-3">Edit <%= @fsp.fsp_number %></h1>
    <p class="page-lede"><%= @fsp.full_name %></p>
  </div>

  <section class="card">
    <div class="card-section">
      <%= render "form", fsp: @fsp %>
    </div>
  </section>
</div>
