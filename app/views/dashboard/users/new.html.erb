<% content_for :title, "Add admin — Hey Tec Inspect" %>

<div class="space-y-6 max-w-xl">
  <div>
    <%= link_to "← All admins", dashboard_users_path, class: "text-sm no-underline text-[--color-ink-muted] hover:text-[--color-ink]" %>
    <h1 class="page-title mt-3">Add admin</h1>
    <p class="page-lede">Create a staff account for the dashboard.</p>
  </div>

  <section class="card">
    <div class="card-section">
      <%= render "form", user: @user %>
    </div>
  </section>
</div>
